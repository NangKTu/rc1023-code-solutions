/* exported lastChars */

/*
Defined a function named lastChar, that accepts two arguments length and string.
  return a string of characters equal to total characters starting from the end.
*/

function lastChars(length, string) {
  return string.slice(-length);
}
