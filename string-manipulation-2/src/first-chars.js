/* exported firstChars */

/*
Defined a function named lastChar, that accepts two arguments length and string.
  return a string of characters equal to total characters starting from beginning.
*/

function firstChars(length, string) {
  return string.slice(0, length);
}
