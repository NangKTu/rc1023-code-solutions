/* exported isUpperCased */

/*
Defined a function named 'isUpperCased', that takes one parameter 'word'.
return result of comparison whether or not 'word' is all uppercase.
*/

function isUpperCased(word) {
  return word === word.toUpperCase();
}
