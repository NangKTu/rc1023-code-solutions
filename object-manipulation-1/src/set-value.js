/* exported setValue */
/*
Defined a function 'setValue', that accepts three arguments 'object', 'key', 'value'
  Update the existing 'object' with new 'key', 'value'
*/

function setValue(object, key, value) {
  object[key] = value;
}
