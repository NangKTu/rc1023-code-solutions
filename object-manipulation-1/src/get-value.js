/* exported getValue */

/*
Defined a function named 'getValue', that accepts two arguments 'object' and 'key'
  return the value of 'object' at 'key'
*/

function getValue(object, key) {
  return object[key];
}
